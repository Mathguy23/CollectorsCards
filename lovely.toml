[manifest]
version = "1.0.0"
dump_lua = true
priority = 0

[[patches]]
[patches.pattern]
target = 'functions/common_events.lua'
pattern = '''elseif specific_vars and (card_type == 'Default' or card_type == 'Enhanced') then'''
position = 'before'
payload = '''elseif specific_vars and type(specific_vars.collect) == "table" then
    full_UI_table.name = {}
    localize{type = 'other', key = specific_vars.collect.key, set = 'Other', nodes = full_UI_table.name, vars = {}}
    full_UI_table.name = full_UI_table.name[1]'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/common_events.lua'
pattern = '''localize{type = 'other', key = _c.key, nodes = desc_nodes, vars = specific_vars or _c.vars}'''
position = 'after'
payload = '''elseif specific_vars and not specific_vars.debuffed and type(specific_vars.collect) == "table" then
    local config_thing = specific_vars.collect.config
    if card and card.ability.trading.key and pc_cross_mod_cards[card.ability.trading.key] and pc_cross_mod_cards[card.ability.trading.key].loc_vars then
        loc_vars = pc_cross_mod_cards[card.ability.trading.key].loc_vars(specific_vars, info_queue, card)
    elseif specific_vars.collect.name == "Flint Card" then
        loc_vars = {localize("Hearts", 'suits_plural'), localize("Diamonds", 'suits_plural'), config_thing.mult}
    elseif specific_vars.collect.name == "Eye Card" then
        loc_vars = {localize("Spades", 'suits_plural'), localize("Clubs", 'suits_plural'), config_thing.chips}
    elseif specific_vars.collect.name == "Scholar's Mate" then
        loc_vars = {localize("Queen", 'ranks'), config_thing.chips, config_thing.mult}
    elseif specific_vars.collect.name == "Scandinavian Defense" then
        loc_vars = {localize("Queen", 'ranks'), config_thing.chips, config_thing.mult, localize("Spades", 'suits_plural'), localize("Clubs", 'suits_plural')}
    elseif specific_vars.collect.name == "Double Up" then
        loc_vars = {config_thing.retriggers}
    elseif specific_vars.collect.name == "Playable Joker" then
        loc_vars = {G.GAME.probabilities.normal, config_thing.odds}
        info_queue[#info_queue + 1] = G.P_CENTERS['e_foil']
        info_queue[#info_queue + 1] = G.P_CENTERS['e_holo']
        info_queue[#info_queue + 1] = G.P_CENTERS['e_polychrome']
    elseif specific_vars.collect.name == "Sunflower" then
        loc_vars = {config_thing.chips, config_thing.gain}
    elseif specific_vars.collect.name == "Rules Card" then
        loc_vars = {config_thing.discards}
    elseif specific_vars.collect.name == "Wild Draw 4" then
        loc_vars = {config_thing.chips, config_thing.cards}
    elseif specific_vars.collect.name == "Jack in a Box" then
        loc_vars = {config_thing.chips, config_thing.scores, config_thing.scored}
    elseif specific_vars.collect.name == "2mbstone" then
        loc_vars = {config_thing.chips, G.GAME.probabilities.normal, config_thing.odds}
        info_queue[#info_queue + 1] = G.P_CENTERS['c_death']
    elseif specific_vars.collect.name == "Mane 6" then
        loc_vars = {config_thing.chips, config_thing.x_mult}
    elseif specific_vars.collect.name == "Blank Card" then
        local ranks = {'', '2', '3', '4', '5', '6', '7', '8', '9', '10', 'Jack', 'Queen', 'King', 'Ace'}
        loc_vars = {config_thing.rank and localize(ranks[config_thing.rank], 'ranks') or "None"}
    elseif specific_vars.collect.name == ":3" then
        info_queue[#info_queue+1] = {key = 'perishable', set = 'Other', vars = {G.GAME.perishable_rounds or 1, G.GAME.perishable_rounds or 1}}
        loc_vars = {config_thing.chips, config_thing.scores, math.max(0, config_thing.scores - config_thing.scored)}
    elseif specific_vars.collect.name == "Golden Ratio" then
        loc_vars = {localize("Diamonds", 'suits_plural'), localize("2", 'ranks'), localize("3", 'ranks'), localize("5", 'ranks'), localize("8", 'ranks'), localize("Ace", 'ranks'), tostring(config_thing.x_mult), config_thing.dollars}
    elseif specific_vars.collect.name == "Pocket Ace" then
        loc_vars = {localize("Spades", 'suits_plural'), localize("Ace", 'ranks'), config_thing.h_chips}
    elseif specific_vars.collect.name == "Haunted Card" then
        info_queue[#info_queue+1] = {key = 'phantom', set = 'Other'}
        loc_vars = {config_thing.cards}
    elseif specific_vars.collect.name == "Executor" then
        loc_vars = {config_thing.chips, config_thing.destroys, config_thing.destroyed}
    elseif specific_vars.collect.name == "Monarch" then
        loc_vars = {config_thing.chips, localize("Hearts", 'suits_plural'), localize("Spades", 'suits_plural'), localize("Clubs", 'suits_plural')}
    elseif specific_vars.collect.name == "Meteor" then
        info_queue[#info_queue + 1] = G.P_CENTERS['m_stone']
        info_queue[#info_queue+1] = {key = 'fleeting', set = 'Other'}
        loc_vars = {config_thing.chips, config_thing.cards}
    elseif specific_vars.collect.name == "Aluminum Plate" then
        loc_vars = {config_thing.h_x_mult}
    elseif specific_vars.collect.name == "Fortune Orb" then
        info_queue[#info_queue+1] = {key = 'phantom', set = 'Other'}
        loc_vars = {config_thing.cards}
    elseif specific_vars.collect.name == "Bust Card" then
        loc_vars = {config_thing.mult, config_thing.cards}
    elseif specific_vars.collect.name == "Old Bell" then
        loc_vars = {localize("Clubs", 'suits_plural'), config_thing.chips, config_thing.x_mult}
    elseif specific_vars.collect.name == "Miscut" then
        loc_vars = {localize("Diamonds", 'suits_plural'), localize("Spades", 'suits_plural'), config_thing.chips}
    elseif specific_vars.collect.name == "Five Fingers" then
        loc_vars = {G.GAME.pc_most_played_poker_hand or "High Card"}
    elseif specific_vars.collect.name == "Full Circle" then
        loc_vars = {config_thing.chips, config_thing.count}
    elseif specific_vars.collect.name == "7 of Luck" then
        loc_vars = { config_thing.chips, G.GAME.probabilities.normal, config_thing.odds, config_thing.mult}
    elseif specific_vars.collect.name == "Top 10" then
        loc_vars = {config_thing.chips, config_thing.mult}
    elseif specific_vars.collect.name == "Crazy Eight" then
        info_queue[#info_queue + 1] = G.P_CENTERS['m_wild']
        loc_vars = {config_thing.mult}
    elseif specific_vars.collect.name == "Dynamic Card" then
        local colours = {G.C.CHIPS, G.C.UI.TEXT_DARK, G.C.MULT, G.C.UI.TEXT_DARK, G.C.MONEY, G.C.FILTER}
        if config_thing.mode == 'chips' then
            colours[1] = G.C.GREEN
            colours[2] = G.C.GREEN
        elseif config_thing.mode == 'mult' then
            colours[3] = G.C.GREEN
            colours[4] = G.C.GREEN
        elseif config_thing.mode == 'money' then
            colours[5] = G.C.GREEN
        elseif config_thing.mode == 'face' then
            colours[6] = G.C.GREEN
        end
        loc_vars = {config_thing.chips, config_thing.mult, config_thing.dollars, colours = colours}
    elseif specific_vars.collect.name == "Big Two" then
        loc_vars = {config_thing.pc_hand_size, config_thing.dollars}
    end
    localize{type = 'descriptions', key = specific_vars.collect.key, set = "CustomCard", nodes = desc_nodes, vars = loc_vars}
    if specific_vars.bonus_chips then
        localize{type = 'other', key = 'card_extra_chips', nodes = desc_nodes, vars = {specific_vars.bonus_chips}}
    end
    if specific_vars and specific_vars.bonus_mult then
        localize{type = 'other', key = 'card_extra_mult', nodes = desc_nodes, vars = {specific_vars.bonus_mult}}
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'game.lua'
pattern = '''self.P_LOCKED = {}'''
position = 'after'
payload = '''self.P_TRADING = {
    flint_card = {order = 1, key = 'flint_card', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Flint Card", pos = {x=1,y=0}, set = "CustomCard", config = {mult = 8}, base = "H_A", rarity = 1},
    scholar_mate = {order = 2, key = 'scholar_mate', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Scholar's Mate", pos = {x=3,y=0}, set = "CustomCard", config = {chips = 10, mult = 12}, base = "S_Q", rarity = 1},
    scandinavian_defense = {order = 3, key = 'scandinavian_defense', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Scandinavian Defense", pos = {x=2,y=0}, set = "CustomCard", config = {chips = 10, mult = 12}, base = "S_Q", rarity = 1},
    double_up = {order = 4, key = 'double_up', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Double Up", pos = {x=3,y=1}, set = "CustomCard", config = {retriggers = 1}, base = "S_Q", rarity = 2},
    eye_card = {order = 5, key = 'eye_card', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Eye Card", pos = {x=4,y=1}, set = "CustomCard", config = {chips = 60}, base = "C_A", rarity = 1},
    playable_joker = {order = 6, key = 'playable_joker', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Playable Joker", pos = {x=0,y=0}, set = "CustomCard", config = {odds = 4}, base = "S_2", rarity = 2},
    sunflower = {order = 7, key = 'sunflower', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Sunflower", pos = {x=0,y=2}, set = "CustomCard", config = {chips = 8, gain = 6}, base = "D_8", rarity = 1},
    rules_card = {order = 8, key = 'rules_card', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Rules Card", pos = {x=1,y=2}, set = "CustomCard", config = {discards = 1}, base = "S_A", rarity = 2},
    wild_draw_4 = {order = 9, key = 'wild_draw_4', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Wild Draw 4", pos = {x=2,y=2}, set = "CustomCard", config = {chips = 4, cards = 4}, base = "C_4", rarity = 1},
    jack_in_a_box = {order = 10, key = 'jack_in_a_box', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Jack in a Box", pos = {x=3,y=2}, set = "CustomCard", config = {chips = 10, scores = 3, scored = 0}, base = "C_J", rarity = 2},
    tombstone = {order = 11, key = 'tombstone', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "2mbstone", pos = {x=4,y=2}, set = "CustomCard", config = {chips = 2, odds = 3}, base = "S_2", rarity = 3},
    mane_6 = {order = 12, key = 'mane_6', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Mane 6", pos = {x=0,y=3}, set = "CustomCard", config = {chips = 6, x_mult = 0.2}, base = "H_6", rarity = 2},
    blank_card = {order = 13, key = 'blank_card', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Blank Card", pos = {x=1,y=3}, set = "CustomCard", config = {}, base = "H_A", rarity = 1},
    colon_3 = {order = 14, key = 'colon_3', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = ":3", pos = {x=2,y=3}, set = "CustomCard", config = {chips = 3, scores = 33, scored = 0}, base = "D_3", rarity = 2},
    golden_ratio = {order = 15, key = 'golden_ratio', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Golden Ratio", pos = {x=3,y=3}, set = "CustomCard", config = {dollars = 1, x_mult = 1.618}, base = "D_2", rarity = 2},
    double_down = {order = 16, key = 'double_down', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Double Down", pos = {x=4,y=3}, set = "CustomCard", config = {}, base = "H_Q", rarity = 2},
    pocket_ace = {order = 17, key = 'pocket_ace', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Pocket Ace", pos = {x=0,y=4}, set = "CustomCard", config = {h_chips = 30}, base = "S_A", rarity = 2},
    haunted_card = {order = 18, key = 'haunted_card', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Haunted Card", pos = {x=1,y=4}, set = "CustomCard", config = {cards = 4}, base = "S_3", rarity = 1},
    executor = {order = 19, key = 'executor', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Executor", pos = {x=2,y=4}, set = "CustomCard", config = {chips = 3, destroys = 3, destroyed = 0}, base = "C_3", rarity = 3},
    monarch = {order = 20, key = 'monarch', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Monarch", pos = {x=3,y=4}, set = "CustomCard", config = {chips = 50}, base = "S_K", rarity = 1},
    meteor = {order = 21, key = 'meteor', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Meteor", pos = {x=4,y=4}, set = "CustomCard", config = {chips = 50, cards = 2}, base = "D_A", rarity = 2},
    aluminum_plate = {order = 22, key = 'aluminum_plate', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Aluminum Plate", pos = {x=0,y=5}, set = "CustomCard", config = {h_x_mult = 2}, base = "S_8", rarity = 2},
    fortune_orb = {order = 23, key = 'fortune_orb', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Fortune Orb", pos = {x=1,y=5}, set = "CustomCard", config = {cards = 2}, base = "S_Q", rarity = 1},
    bust_card = {order = 24, key = 'bust_card', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Bust Card", pos = {x=2,y=5}, set = "CustomCard", config = {mult = 22, cards = 2}, base = "H_2", rarity = 2},
    old_bell = {order = 25, key = 'old_bell', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Old Bell", pos = {x=3,y=5}, set = "CustomCard", config = {chips = 10, x_mult = 2}, base = "C_K", rarity = 3},
    miscut = {order = 26, key = 'miscut', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Miscut", pos = {x=4,y=5}, set = "CustomCard", config = {chips = 11}, base = "S_A", rarity = 1},
    free_pass = {order = 27, key = 'free_pass', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Free Pass", pos = {x=0,y=6}, set = "CustomCard", config = {}, base = "H_5", rarity = 2},
    five_fingers = {order = 28, key = 'five_fingers', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Five Fingers", pos = {x=1,y=6}, set = "CustomCard", config = {}, base = "D_5", rarity = 1},
    full_circle = {order = 29, key = 'full_circle', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Full Circle", pos = {x=2,y=6}, set = "CustomCard", config = {chips = 9, count = 0, saved = {}}, base = "S_9", rarity = 2},
    seven_luck = {order = 30, key = 'seven_luck', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "7 of Luck", pos = {x=3,y=6}, set = "CustomCard", config = {chips = 7, mult = 20, odds = 7}, base = "C_7", rarity = 2},
    top_ten = {order = 31, key = 'top_ten', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Top 10", pos = {x=4,y=6}, set = "CustomCard", config = {chips = 10, mult = 5}, base = "S_T", rarity = 2},
    crazy_eight = {order = 32, key = 'crazy_eight', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Crazy Eight", pos = {x=0,y=7}, set = "CustomCard", config = {mult = 8}, base = "S_8", rarity = 2},
    dynamic_card = {order = 33, key = 'dynamic_card', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Dynamic Card", pos = {x=1,y=7}, set = "CustomCard", config = {mult = 4, chips = 30, dollars = 2}, base = "H_6", rarity = 1},
    big_two = {order = 34, key = 'big_two', unlocked = true, discovered = true, atlas = 'pc_trading', cost = 1, name = "Big Two", pos = {x=2,y=7}, set = "CustomCard", config = {pc_debuffed = false, pc_hand_size = 2, dollars = 2}, base = "S_2", rarity = 3},
}'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/common_events.lua'
pattern = '''if not strip_edition then'''
position = 'before'
payload = '''new_card:set_sprites(new_card.config.center)'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''function Card:is_suit(suit, bypass_debuff, flush_calc)'''
position = 'after'
payload = '''   if self.ability and self.ability.trading then
        local eval = self:calculate_exotic({bypass_debuff = bypass_debuff, flush_calc = flush_calc, is_suit = suit})
        if (eval ~= nil) then
            return eval
        end
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''function Card:get_id()'''
position = 'after'
payload = '''   if self.ability and self.ability.trading then
        local eval = self:calculate_exotic({get_id = true})
        if eval then
            return eval
        end
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''function Card:can_use_consumeable(any_state, skip_check)'''
position = 'after'
payload = '''   if (self.ability.name == 'Strength') or self.ability.consumeable.suit_conv then 
        for i = 1, #G.hand.highlighted do
            if G.hand.highlighted[i].ability and G.hand.highlighted[i].ability.trading then return false end
        end
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'game.lua'
pattern = '''table.sort(self.P_CENTER_POOLS["Seal"], function (a, b) return a.order < b.order end)'''
position = 'after'
payload = '''table.sort(self.P_CENTER_POOLS["Exotic"], function (a, b) return a.order < b.order end)'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'game.lua'
pattern = '''for k, v in pairs(self.P_STAKES) do'''
position = 'before'
payload = '''for k, v in pairs(self.P_TRADING) do
    v.key = k
    if not v.wip and not v.demo then 
        table.insert(self.P_CENTER_POOLS["Exotic"], v)
    end
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'game.lua'
pattern = '''Stake = {},'''
position = 'after'
payload = '''Exotic = {},'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''function Card:set_ability(center, initial, delay_sprites)'''
position = 'after'
payload = '''   if self.force_trading and (center == G.P_CENTERS["m_pc_trading"]) then
        local key = G.P_TRADING[self.force_trading]
        self.force_trading = nil
        self.ability.trading = copy_table(key)
    elseif self.ability and self.ability.trading and (center ~= G.P_CENTERS["m_pc_trading"]) then
        self.ability.trading = nil
    elseif self.ability and not self.ability.trading and (center == G.P_CENTERS["m_pc_trading"]) then
        local key = G.P_TRADING[get_trading_key()]
        self.force_trading = nil
        self.ability.trading = copy_table(key)
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''local cards = {}'''
position = 'before'
payload = '''for j = 1, #G.hand.cards do
    G.hand.cards[j]:calculate_exotic({pre_discard = true, full_hand = G.hand.highlighted, hook = hook}, {})
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''local hand_text_set = false'''
position = 'before'
payload = '''for k=1, #G.play.cards do
    G.play.cards[k]:calculate_exotic({cardarea = G.play, full_hand = G.play.cards, scoring_hand = scoring_hand, scoring_name = text, poker_hands = poker_hands, before = true})
end
for k=1, #G.hand.cards do
    G.hand.cards[k]:calculate_exotic({cardarea = G.hand, full_hand = G.play.cards, scoring_hand = scoring_hand, scoring_name = text, poker_hands = poker_hands, before = true})
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''function Card:is_face(from_boss)'''
position = 'after'
payload = '''if self.ability and self.ability.trading then
        local eval = self:calculate_exotic({from_boss = from_boss, is_face = true})
        if eval then
            return eval
        end
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''if self.children.use_button then self.children.use_button:remove(); self.children.use_button = nil end'''
position = 'after'
payload = '''if self.children.rank_button then self.children.rank_button:remove(); self.children.rank_button = nil end
if self.children.dynamic_button then self.children.dynamic_button:remove(); self.children.dynamic_button = nil end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''self.highlighted = is_higlighted'''
position = 'after'
payload = '''if self.playing_card then
    if self.highlighted and self.ability and self.ability.trading and (self.ability.trading.name == "Blank Card") and (self.area == G.hand) then
        self.children.rank_button = UIBox{
            definition = G.UIDEF.rank_buttons(self), 
            config = {align=
                    "bmi"
                , offset =
                    {x=0,y=0.65},
                parent =self}
        }
    elseif self.children.rank_button then
        self.children.rank_button:remove()
        self.children.rank_button = nil
    end
    if self.highlighted and self.ability and self.ability.trading and (self.ability.trading.name == "Dynamic Card") and (self.area == G.hand) then
        self.children.dynamic_button = UIBox{
            definition = G.UIDEF.pc_dynamic_buttons(self), 
            config = {align=
                    "cm"
                , offset = 
                    {x= 0,y= 0},
                parent =self}
        }
    elseif self.children.dynamic_button then
        self.children.dynamic_button:remove()
        self.children.dynamic_button = nil
    end
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''local cards_destroyed = {}'''
position = 'after'
payload = '''for i = 1, #scoring_hand do
    for j = 1, #scoring_hand do
        if scoring_hand[j]:calculate_exotic({destroying_card = scoring_hand[i], full_hand = G.play.cards, cardarea = G.play, scoring_name = text}) == true then 
            if SMODS.shatters(scoring_hand[i]) then
                scoring_hand[i].shattered = true
            else
                scoring_hand[i].destroyed = true
            end
            cards_destroyed[#cards_destroyed + 1] = scoring_hand[i]
        end
    end
    for j = 1, #G.hand.cards do
        if G.hand.cards[j]:calculate_exotic({destroying_card = scoring_hand[i], full_hand = G.play.cards, cardarea = G.hand, scoring_name = text}) == true then 
            if SMODS.shatters(scoring_hand[i]) then
                scoring_hand[i].shattered = true
            else
                scoring_hand[i].destroyed = true
            end
            cards_destroyed[#cards_destroyed + 1] = scoring_hand[i]
        end
    end
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''-- context.remove_playing_cards calculations'''
position = 'before'
payload = '''local remove_indexes = {}
for i = 1, #cards_destroyed do
    for j = i + 1, #cards_destroyed do
        if (i ~= j) and (cards_destroyed[i] == cards_destroyed[j]) then
            for k = 1, #remove_indexes do
                if remove_indexes[k] == i then
                    break
                end
            end
            table.insert(remove_indexes, i)
            break
        end
    end
end
for i = #remove_indexes, -1, 1 do
    table.remove(cards_destroyed, remove_indexes[i])
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''function Card:set_perishable(_perishable)'''
position = 'after'
payload = '''   if self.ability.force_perish then
        return
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''check_for_unlock({type = 'hand_contents', cards = G.play.cards})'''
position = 'after'
payload = '''G.E_MANAGER:add_event(Event({
    trigger = 'immediate',
        func = function()
            for k=1, #G.play.cards do
                if not G.play.cards[k].ability.pc_times_triggered then
                    G.play.cards[k].ability.pc_times_triggered = 1
                else
                    G.play.cards[k].ability.pc_times_triggered = G.play.cards[k].ability.pc_times_triggered + 1
                end
            end
            pc_redo_top10()
            local pool = {}
            for k=1, #G.play.cards do
                table.insert(pool, G.play.cards[k])
                G.play.cards[k]:calculate_exotic({cardarea = G.play, full_hand = G.play.cards, very_before = true})
            end
            while #pool > 0 do
                local card, key = pseudorandom_element(pool, pseudoseed('order'))
                table.remove(pool, key)
                card:calculate_exotic({cardarea = G.play, full_hand = G.play.cards, very_before = true, random_order = true})
            end
        return true
    end
}))'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''if G.GAME.modifiers.debuff_played_cards then'''
position = 'before'
payload = '''for k=1, #G.play.cards do
    G.play.cards[k]:calculate_exotic({cardarea = G.play, full_hand = G.play.cards, scoring_hand = scoring_hand, scoring_name = text, poker_hands = poker_hands, after = true})
end
for k=1, #G.hand.cards do
    G.hand.cards[k]:calculate_exotic({cardarea = G.hand, full_hand = G.play.cards, scoring_hand = scoring_hand, scoring_name = text, poker_hands = poker_hands, after = true})
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''for i=1, #G.hand.cards do'''
position = 'after'
payload = '''   local pc_in_hand = true'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''for i=1, #scoring_hand do'''
position = 'after'
payload = '''   local pc_in_scoring = true'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/common_events.lua'
pattern = '''if context.repetition_only then'''
position = 'before'
payload = '''local special_eval = nil
    if context.discard and (context.full_hand == G.hand.highlighted) and not context.no_trading then
        special_eval = card:calculate_exotic({discard = true, full_hand = G.hand.highlighted, other_card = context.other_card})
    end
    if special_eval and special_eval.remove then
        ret.remove = true
    end
if not context.no_trading then
    local new_context = {no_trading = true}
    for i, j in pairs(context) do
        new_context[i] = j
    end
    local effects = {eval_card(card, new_context)}
    local new_table = {}
    if context.main_scoring then
        if context.scoring_hand then
            for k=1, #context.scoring_hand do
                local eval = context.scoring_hand[k]:calculate_exotic({cardarea = context.cardarea, full_hand = G.play.cards, scoring_hand = context.scoring_hand, scoring_name = context.scoring_name, poker_hands = context.poker_hands, other_card = card, individual = true}, {})
                if eval then 
                    for l = 1, #eval do
                        table.insert(new_table, eval[l])
                    end
                end
            end
        end
        for k=1, #G.hand.cards do
            local eval = G.hand.cards[k]:calculate_exotic({cardarea = context.cardarea, full_hand = G.play.cards, scoring_hand = context.scoring_hand, scoring_name = context.scoring_name, poker_hands = context.poker_hands, other_card = card, individual = true}, {})
            if eval then 
                for l = 1, #eval do
                    table.insert(new_table, eval[l])
                end
            end
        end
        local exotic_eval = card:calculate_exotic({cardarea = context.cardarea, full_hand = G.play.cards, scoring_hand = context.scoring_hand, scoring_name = context.scoring_name, poker_hands = context.poker_hands, playing_card_hand = (context.cardarea == G.hand), playing_card_main = (context.cardarea == G.play)})
        if exotic_eval then
            for l = 1, #exotic_eval do
                table.insert(new_table, exotic_eval[l])
            end
        end
    end
    if context.end_of_round and (context.cardarea == G.hand) and not context.individual and not context.repetition and not context.playing_card_end_of_round then
        for k=1, #G.hand.cards do
            local eval = G.hand.cards[k]:calculate_exotic({cardarea = context.cardarea, other_card = card, individual = true, end_of_round = true}, {})
            if eval and next(eval) then 
                for l = 1, #eval do
                    table.insert(new_table, eval[l])
                end
            end
        end
        local exotic_eval = card:calculate_exotic({cardarea = context.cardarea, end_of_round = true, game_over = context.game_over}, {})
        if exotic_eval then
            for l = 1, #exotic_eval do
                table.insert(new_table, exotic_eval[l])
            end
        end
    end
    if next(new_table) then
        effects[1]['trading_cards'] = new_table
    end
    return unpack(effects)
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''function end_round()'''
position = 'after'
payload = '''   local pc_end_round = true'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''G.RESET_BLIND_STATES = true'''
position = 'after'
payload = '''local delete_these = {}
for i = 1, #G.playing_cards do
    if G.playing_cards[i].ability.fleeting then
        table.insert(delete_these, G.playing_cards[i])
    end
    G.playing_cards[i].ability.pc_drawn_to_hand = nil
end
for i = 1, #delete_these do
    delete_these[i]:remove()
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''local badges = {}'''
position = 'after'
payload = '''if loc_vars then loc_vars.collect = (self.ability and self.ability.trading or nil) end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''G.FUNCS.play_cards_from_highlighted = function(e)'''
position = 'after'
payload = '''   
    for i = 1, #G.hand.highlighted do
        if G.hand.highlighted[i].ability and G.hand.highlighted[i].ability.phantom then
            G.hand.highlighted[i]:juice_up()
            G.E_MANAGER:add_event(Event({
                trigger = 'immediate',
                func = function()
                    G.STATE = G.STATES.DRAW_TO_HAND
                    G.STATE_COMPLETE = false
                    return true
                end
            }))
            return
        end
    end
    for i = 1, #G.hand.cards do
        if G.hand.cards[i].ability and G.hand.cards[i].ability.phantom then
            G.hand.cards[i].ability.phantom = nil
        end
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/common_events.lua'
pattern = '''--Fill all remaining info if this is the main desc'''
position = 'before'
payload = '''if card and card.ability and card.ability.phantom then info_queue[#info_queue+1] = {key = 'phantom', set = 'Other'} end
if card and card.ability and card.ability.fleeting then info_queue[#info_queue+1] = {key = 'fleeting', set = 'Other'} end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''G.FUNCS.discard_cards_from_highlighted = function(e, hook)'''
position = 'after'
payload = '''   for i = 1, #G.hand.cards do
        if G.hand.cards[i].ability and G.hand.cards[i].ability.phantom then
            G.hand.cards[i].ability.phantom = nil
        end
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'blind.lua'
pattern = '''function Blind:stay_flipped(area, card)'''
position = 'after'
payload = '''   
    if phantom_cards and (area == G.hand) then
        card.ability.phantom = true
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'blind.lua'
pattern = '''function Blind:stay_flipped(area, card, from_area)'''
position = 'after'
payload = '''   
    if phantom_cards and (area == G.hand) then
        card.ability.phantom = true
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'game.lua'
pattern = '''G.GAME.blind:drawn_to_hand()'''
position = 'before'
payload = '''-- phantom_cards = nil
--for i = 1, #G.hand.cards do
--    if G.hand.cards[i].ability.trading and (G.hand.cards[i].ability.already_drawn ~= G.GAME.round) then
--        G.hand.cards[i]:calculate_exotic({drawn = G.hand, facing_blind = G.GAME.facing_blind})
--    end
--end
'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''if G.GAME.blind.name == 'The Serpent' and'''
position = 'before'
payload = '''if G.GAME.pc_hand_size_bonus then
    if hand_space <= 0 then
        hand_space = math.max(0, math.min(G.hand.config.card_limit - #G.hand.cards, 0) + G.GAME.pc_hand_size_bonus)
    else
        hand_space = hand_space + G.GAME.pc_hand_size_bonus
    end
    G.GAME.pc_hand_size_bonus = nil
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'cardarea.lua'
pattern = '''if area == G.discard then'''
position = 'before'
payload = '''card.ability.pc_drawn_to_hand = true'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/common_events.lua'
pattern = '''function copy_card(other, new_card, card_scale, playing_card, strip_edition)'''
position = 'after'
payload = '''   local old_fleeting = other.ability and other.ability.fleeting or nil
    if new_card and new_card.ability then
        old_fleeting = new_card.ability.fleeting
    end
    local new_phantom = other.ability and other.ability.phantom or nil'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/common_events.lua'
pattern = '''new_card:set_sprites(new_card.config.center)'''
position = 'before'
payload = '''new_card.ability.fleeting = old_fleeting
new_card.ability.phantom = new_phantom'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'game.lua'
pattern = '''if _de.gold_seal then _g = _de.gold_seal end'''
position = 'after'
payload = '''if _de.trade then _t = _de.trade end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'game.lua'
pattern = '''local keep, _e, _d, _g = true, nil, nil, nil'''
position = 'after'
payload = '''local _t = nil'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'game.lua'
pattern = '''if keep then card_protos[#card_protos+1] = {s=_s,r=_r,e=_e,d=_d,g=_g} end'''
position = 'after'
payload = '''if keep then card_protos[#card_protos].t = {t = _t} end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/misc_functions.lua'
pattern = '''if control.g then _card:set_seal(control.g, true, true) end'''
position = 'after'
payload = '''if control.t then _card.ability.trading = G.P_TRADING[control.t];_card:set_sprites(_card.config.center) end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/UI_definitions.lua'
pattern = '''if v.g then _card:set_seal(v.g, true, true) end'''
position = 'after'
payload = '''if v.t then _card.ability.trading = G.P_TRADING[v.t];_card:set_sprites(_card.config.center)  end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/button_callbacks.lua'
pattern = '''e.config.ref_table:use_consumeable(area)'''
position = 'after'
payload = '''for i = 1, #G.hand.cards do
    G.hand.cards[i]:calculate_exotic({using_consumeable = e.config.ref_table}, {})
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''v.ability.discarded = nil'''
position = 'after'
payload = '''if v.debuff then
    v.ability.temp_debuff = nil
    v:set_debuff()
end
if v.ability.temp_no_debuff then
    v.ability.temp_no_debuff = nil
    v:set_debuff()
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'cardarea.lua'
pattern = '''if v.ability.forced_selection and not self.highlighted[1] then'''
position = 'before'
payload = '''if v.ability.trading and (v.ability.trading.name == "Old Bell") and not v.highlighted and not v.debuff and not (v.ability.forced_selection and not self.highlighted[1]) and G.STATE == G.STATES.SELECTING_HAND then
    self:add_to_highlighted(v)
    if v.highlighted then
        v.ability.forced_selection = true
    end
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'game.lua'
pattern = '''function Game:init_item_prototypes()'''
position = 'before'
payload = '''function pc_add_cross_mod_card(args)
    pc_cross_mod_cards = pc_cross_mod_cards or {}
    G.P_TRADING[args.key] = args.card
    table.insert(G.P_CENTER_POOLS["Exotic"], G.P_TRADING[args.key])
    local order = #G.P_CENTER_POOLS["Exotic"]
    G.P_TRADING[args.key].order = order + 1
    G.P_TRADING[args.key].set = "CustomCard"
    pc_cross_mod_cards[args.key] = {center = G.P_TRADING[args.key]}
    table.sort(G.P_CENTER_POOLS["Exotic"], function (a, b) return a.order < b.order end)
    if args.calculate then
        pc_cross_mod_cards[args.key].calculate = args.calculate
    end
    if args.loc_vars then
        pc_cross_mod_cards[args.key].loc_vars = args.loc_vars
    end
    if args.in_pool then
        pc_cross_mod_cards[args.key].in_pool = args.in_pool
    end
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''ease_hands_played(-1)'''
position = 'at'
payload = '''for k=1, #G.hand.highlighted do
    G.hand.highlighted[k]:calculate_exotic({cardarea = G.hand, full_hand = G.hand.highlighted, very_very_before = true})
end
if not hand_is_free then
    ease_hands_played(-1)
end
hand_is_free = nil'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''play_area_status_text("Not Allowed!")--localize('k_not_allowed_ex'), true)'''
position = 'after'
payload = '''for k=1, #scoring_hand do
    scoring_hand[k]:calculate_exotic({cardarea = G.play, debuffed_hand = true})
end
for k=1, #G.hand.cards do
    G.hand.cards[k]:calculate_exotic({cardarea = G.hand, debuffed_hand = true})
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''G.RESET_JIGGLES = true'''
position = 'after'
payload = '''
G.GAME.blind_attack = nil
local skill_saved = false
for i = 1, #G.playing_cards do
    G.playing_cards[i]:calculate_exotic({total_end_of_round = true})
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/button_callbacks.lua'
pattern = '''card:sell_card()'''
position = 'after'
payload = '''if G.hand and G.hand.cards and G.hand.cards[1] then
    for i = 1, #G.hand.cards do
        G.hand.cards[i]:calculate_exotic({selling_card = card})
    end
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''G.GAME.blind:set_blind(G.GAME.round_resets.blind)'''
position = 'after'
payload = '''for i = 1, #G.playing_cards do
    G.playing_cards[i]:calculate_exotic({setting_blind = true, blind = G.GAME.round_resets.blind})
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''SMODS.calculate_context({open_booster = true, card = self})'''
position = 'before'
payload = '''for i = 1, #G.playing_cards do
    G.playing_cards[i]:calculate_exotic({open_booster = true, card = self})
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/button_callbacks.lua'
pattern = '''G.CONTROLLER:recall_cardarea_focus('shop_jokers')'''
position = 'after'
payload = '''for i = 1, #G.playing_cards do
    G.playing_cards[i]:calculate_exotic({shop_reroll = true, cost = reroll_cost})
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/button_callbacks.lua'
pattern = '''G.FUNCS.skip_booster = function(e)'''
position = 'after'
payload = '''for i = 1, #G.playing_cards do
    G.playing_cards[i]:calculate_exotic({skipping_booster = true, card = e.config.ref_table})
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''G.GAME.hands[text].visible = true'''
position = 'before'
payload = '''local _handname, _played, _order = 'High Card', -1, 100
for k, v in pairs(G.GAME.hands) do
    if v.played > _played or (v.played == _played and _order > v.order) then 
        _played = v.played
        _handname = k
    end
end
G.GAME.pc_most_played_poker_hand = _handname'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'game.lua'
pattern = '''if not G.GAME.current_round.used_packs[i] then'''
position = 'before'
payload = '''if not G.GAME.current_round.used_packs[1] then
    if G.GAME.force_mega_trading then
        G.GAME.current_round.used_packs[1] = 'p_pc_trading_mega_1'
    end
    if not G.GAME.current_round.used_packs[1] then
        G.GAME.current_round.used_packs[1] = get_pack('shop_pack').key
    end
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'game.lua'
pattern = '''create_shop_card_ui(card, 'Booster', G.shop_booster)'''
position = 'before'
payload = '''if G.GAME.force_mega_trading then
    card.ability.pc_free = true
    G.GAME.force_mega_trading = nil
    card:set_cost()
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/button_callbacks.lua'
pattern = '''G.deck:shuffle('cashout'..G.GAME.round_resets.ante)'''
position = 'before'
payload = '''if (G.GAME.selected_back.name == "Enthusiast Deck") and (G.GAME.blind_on_deck == "Boss") then
    G.GAME.force_mega_trading = true
else
    G.GAME.force_mega_trading = nil
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''self.cost = math.max(1, math.floor((self.base_cost + self.extra_cost + 0.5)*(100-G.GAME.discount_percent)/100))'''
position = 'after'
payload = '''if self.ability.pc_free then
    self.cost = 0
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/UI_definitions.lua'
pattern = '''local badges = {}'''
position = 'after'
payload = '''if card and card.ability and card.ability.trading and card.ability.trading.rarity then
    local rarities = {localize('k_shabby'), localize('k_basic'), localize('k_mint')}
    local colors = {G.ARGS.LOC_COLOURS.cp_shabby, G.ARGS.LOC_COLOURS.cp_basic, G.ARGS.LOC_COLOURS.cp_mint}
    if rarities[card.ability.trading.rarity] then
        badges[#badges + 1] = create_badge(rarities[card.ability.trading.rarity], colors[card.ability.trading.rarity], nil, 1.2)
    end
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/misc_functions.lua'
pattern = '''money = G.C.MONEY,'''
position = 'after'
payload = '''cp_shabby = HEX('7c6215'),
cp_basic = HEX('16a91b'),
cp_mint = HEX('05b6b6'),'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''local badges = {}'''
position = 'after'
payload = '''if self.ability and self.ability.trading then
    no_badge = nil
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''badges.card_type = card_type'''
position = 'at'
payload = '''if not self.ability or not self.ability.trading then
    badges.card_type = card_type
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'card.lua'
pattern = '''if self.ability.name == "Joker Stencil" then'''
position = 'before'
payload = '''if (self.area == G.hand) and self.ability and self.ability.trading and self.ability.trading.config.pc_hand_size then
    local curr_debuff = self.debuff
    local old_debuff = self.ability.trading.config.pc_debuffed
    if curr_debuff == nil then
        curr_debuff = false
    else
        curr_debuff = true
    end
    if curr_debuff ~= old_debuff then
        self.ability.trading.config.pc_debuffed = curr_debuff
        if self.ability.trading.config.pc_hand_size then
            if curr_debuff then
                G.hand.config.real_card_limit = (G.hand.config.real_card_limit or G.hand.config.card_limit) - self.ability.trading.config.pc_hand_size
                G.hand.config.card_limit = math.max(0, G.hand.config.real_card_limit)
            else
                G.hand.config.real_card_limit = (G.hand.config.real_card_limit or G.hand.config.card_limit) + self.ability.trading.config.pc_hand_size
                G.hand.config.card_limit = math.max(0, G.hand.config.real_card_limit)
            end
        end
    end
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'cardarea.lua'
pattern = '''if self.cards[i] == card then'''
position = 'after'
payload = '''   if card.ability and card.ability.trading and card.ability.trading.config.pc_hand_size and (self == G.hand) then
        self.config.real_card_limit = (self.config.real_card_limit or self.config.card_limit) - card.ability.trading.config.pc_hand_size
        self.config.card_limit = math.max(0, self.config.real_card_limit)
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'cardarea.lua'
pattern = '''function CardArea:emplace(card, location, stay_flipped)'''
position = 'after'
payload = '''   if card.ability and card.ability.trading and card.ability.trading.config.pc_hand_size and not card.debuff and (self == G.hand) then
        self.config.real_card_limit = (self.config.real_card_limit or self.config.card_limit) + card.ability.trading.config.pc_hand_size
        self.config.card_limit = math.max(0, self.config.real_card_limit)
    end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''if limit <= 0 then break end'''
position = 'before'
payload = '''if not card.debuff and card.ability and card.ability.trading and card.ability.trading.config.pc_hand_size and not card.debuff then
    if limit + card.ability.trading.config.pc_hand_size < 0 then
        if pc_old_limit == 0 then
            limit = -1
        else
            limit = 0
        end
    else
        limit = limit + card.ability.trading.config.pc_hand_size
    end
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''draw_card(G.deck,G.hand, i*100/hand_space,'up', true, cards_to_draw[i])'''
position = 'after'
payload = '''cards_to_draw[i]:calculate_exotic({drawn = G.hand, facing_blind = G.GAME.facing_blind, drawn_cards = cards_to_draw})'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/common_events.lua'
pattern = '''for k, v in pairs(other.ability) do'''
position = 'before'
payload = '''local pc_hand_size1 = not new_card.debuff and new_card.ability and new_card.ability.trading and new_card.ability.trading.config.pc_hand_size or 0'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/common_events.lua'
pattern = '''if not strip_edition then'''
position = 'before'
payload = '''local pc_hand_size2 = not new_card.debuff and new_card.ability and new_card.ability.trading and new_card.ability.trading.config.pc_hand_size or 0
if new_card.ability and new_card.ability.trading and new_card.reset_trading_progress then
    new_card:reset_trading_progress()
end
if new_card.area == G.hand then
    if pc_hand_size1 ~= pc_hand_size2 then
        local change = pc_hand_size2 - pc_hand_size1
        G.hand.config.real_card_limit = (G.hand.config.real_card_limit or G.hand.config.card_limit) + change
        G.hand.config.card_limit = math.max(0, G.hand.config.real_card_limit)
    end
end'''
match_indent = true

[[patches]]
[patches.pattern]
target = 'functions/state_events.lua'
pattern = '''limit = limit - 1 + mod'''
position = 'before'
payload = '''local pc_old_limit = limit'''
match_indent = true